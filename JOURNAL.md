# Journal

Append-only. One primary entry per iteration; SALVAGE and ROTATION entries are additional. Never rewrite past entries (filling the current entry's Checkpoint field is completion, not a rewrite).

Heading grammar, exactly (fenced and indented here so this example is never mistaken for an entry by anything that counts or rotates them):

```
  ## iter <i>/<N> | <run-id> | <YYYY-MM-DD> | <task-id or AUDIT or EVALUATOR or RATCHET or WRAPUP or SALVAGE or ROTATION> | <done|blocked|audit|converged|salvage|rotation>
```

Write a real heading at column zero, never indented: the indentation above belongs to the example alone, and an indented heading is invisible to the rotation anchor and to the archive counter, so the entry under it is not counted and not rotated.

SALVAGE entries take status salvage; ROTATION entries take status rotation. An EVALUATOR entry records an evaluator-gate iteration: status audit when the run continues after the verdict, blocked on a terminal second REJECT, converged when that same iteration declares.

run-id is the first 8 characters of the session id, a hyphen, then the HHMMSS of started_at from the loop state frontmatter, so two runs in one session are told apart. Body fields, in order: Task, Changed, Checkpoint (the jeffy checkpoint commit hash, or none with the reason), Verification, Learnings, Next.

The closing entry that declares convergence carries the evaluator verdict in its Verification field: `Evaluator: PASS - <one-line summary>`, or `Evaluator: unavailable (<reason>)`. An earlier EVALUATOR entry records its own verdict the same way and never stands in for the closing one: the Stop hook reads the closing entry alone, so a run that gates early and keeps working re-invokes the gate at the declaration.

Closed tasks are recorded here as one line each (ID, title, closing evidence), because BACKLOG.md deletes them. Rotation: when this file exceeds 500 lines, move all but the last 10 entries to the end of JOURNAL-archive.md, appending to whatever that file already holds and never overwriting it, because the archive accumulates across every rotation and every run; create it only when it does not already exist, and record the rotation as a ROTATION entry.

## iter 1/12 | cd1c2080-020939 | 2026-08-04 | AUDIT | audit

Task: Scoped-mode derivation audit. Greenfield tree (tracked: .gitattributes, .gitignore, PLAN.md, BACKLOG.md only); derived the full task set from the Definition of done per the Method (harness before matcher).
Changed: BACKLOG.md seeded with 15 open tasks: Now = scaffold, corpus-freeze, harness; Next = the 12 frozen inventory rows in frozen order. JOURNAL.md bootstrapped from template this iteration.
Checkpoint: 3033c98fcf470ae6a0134a7a3a0e371c98e484d5. Stall check: audit iteration, 15 backlog items changed state (created open), no stall.
Verification: Toolchain verified: cargo 1.90.0, git 2.50.1.windows.1 - exactly the version the oracle contract was pre-registered against. Verify command payload passes bash -n. Scores: High 15 (DoD items 1-5 wholly unmet: no build, no corpus, no harness, 12 unswept rows), Medium 0, Low 0. All findings filed as tasks; nothing in-envelope left unfiled.
Learnings: Oracle git version on this machine matches the pre-registration version string byte for byte, so no version-drift caveat is needed in the harness.
Next: scaffold - create the crate skeleton so the Verify command can run at all.

## iter 2/12 | cd1c2080-020939 | 2026-08-04 | scaffold | done

Task: scaffold (High, build-ci) - crate skeleton so the Verify command can run. Closed: created package gitignore-matcher (edition 2024, zero dependencies) with lib Matcher stub (matches nothing, to be observed failing by the harness) and differential bin parsing --corpus <dir> --strict, exiting 2 with a not-implemented message.
Changed: Cargo.toml, Cargo.lock, src/lib.rs, src/bin/differential.rs created. BACKLOG.md: scaffold line deleted.
Checkpoint: c32d3d7d4df61aa5a883528f0fa85fdd4c1df00f. Stall check: 4 source files created outside state files, scaffold changed state open -> closed, no stall.
Verification: Acceptance: cargo build --release exit 0, observed. Verify command: exit 2 (harness stub not implemented) - red at last checkpoint too (no project existed), so not a regression; expected red until harness and corpus land. cargo tree: gitignore-matcher v0.1.0 alone, no gitignore-implementing dependency.
Learnings: TMPDIR is unset in this Git Bash; redirect logs to the session scratchpad path, never $TMPDIR.
Next: corpus-freeze - generate and freeze the 12 slices, record counts in PLAN.md.

## iter 3/12 | cd1c2080-020939 | 2026-08-04 | corpus-freeze | done

Task: corpus-freeze (High, test) - generate and freeze the corpus. Closed: corpus_gen bin wrote 12 slices named exactly per the frozen inventory rows, 53 cases, 123 queries, NUL-terminated token records (F/D/E/X/Q), inputs only - verdicts come from the live oracle at replay. Generator refuses to run when corpus/ exists (freeze guard, observed exit 1 on rerun).
Changed: src/bin/corpus_gen.rs created; corpus/ (53 case files in 12 dirs) created; PLAN.md frozen counts line added under the inventory; BACKLOG.md corpus-freeze line deleted.
Checkpoint: 3d1a0d395c65a5de58ac1944b71a708cc35227c3. Stall check: corpus/ (53 files) and corpus_gen.rs created outside state files, corpus-freeze changed state open -> closed, no stall.
Verification: Acceptance: ls corpus shows the 12 inventory slice names, all non-empty (4/4/4/5/4/5/7/3/3/3/4/7 cases); od -c confirms NUL delimiting with tab and trailing-space bytes intact; counts recorded in PLAN.md. Freeze guard observed: second corpus_gen run exits 1 without touching corpus/. Verify command: still exit 2 (harness stub), red at last checkpoint too, not a regression. cargo tree: gitignore-matcher alone, no gitignore-implementing dependency.
Learnings: corpus/** -text in .gitattributes was pre-registered and od confirms it: NULs, tabs, and trailing spaces survive byte-exact through git.
Next: harness - implement the oracle contract in the differential bin; first run must show the stub matcher failing.

## iter 4/12 | cd1c2080-020939 | 2026-08-04 | harness | done

Task: harness (High, test) - differential driver per the oracle contract. Closed: one temp repo per case under the system temp dir (never this tree), config-clean env on every git call, -z -v --non-matching --no-index --stdin invocation, four-field NUL parse validated against query order, negation-aware verdict rule, oracle exit 128 = Err at the run_oracle choke point every case flows through; exit codes 0 agree / 1 disagree / 3 harness failure, empty corpus refuses to read as green.
Changed: src/bin/differential.rs rewritten from stub to full driver plus two unit tests; src/lib.rs Matcher API changed from bare stub to source-accumulating builder (add_gitignore, set_info_exclude, set_excludes_file, is_ignored(rel_path, is_dir)); contract preserved: is_ignored still answers false for every path until row tasks land, callers = differential bin only, no swept inventory rows exist to flip. BACKLOG.md harness line deleted.
Checkpoint: af75a5ba7ea0fda1169fe40ae9d71a3a0d1640b8. Stall check: differential.rs and lib.rs changed outside state files, harness changed state open -> closed, no stall.
Verification: Acceptance observed: full replay = 53 cases, 123 queries, 69 disagreements, 0 harness failures, exit 1 - the stub failing first, every slice showing disagreements, each DISAGREE line carrying git's pattern, source, and lineno. cargo test --release --bin differential: 2 passed (oracle-outside-repo exit 128 -> Err naming 128; verdict rule end-to-end with real git, negation evidence carries !keep.log). Strict abort probed with a malformed scratch case (never the frozen corpus): exit 3 with HARNESS FAILURE line; non-strict continues and still exits 3. The probe caught a real off-by-one: truncated record panicked (exit 101) before the fix, clean harness failure after; fix attempt 2 of 3. cargo tree: gitignore-matcher alone.
Learnings: The strict-abort probe against a deliberately malformed scratch case caught a bounds-check panic the happy path never would; keep probing harness failure paths with inputs the corpus cannot contain.
Next: row-blank-and-comment - first matcher semantics; its slice currently shows 3 disagreements to eliminate.

## iter 5/12 | cd1c2080-020939 | 2026-08-04 | row-blank-and-comment | done

Task: row-blank-and-comment (High, runtime) - first matcher semantics plus the matcher core they require. Closed: lib.rs implements gitignore line parsing (blank skip, # comment skip, dir.c trim_trailing_spaces port, ! negation flag, trailing-/ dir_only flag, separator-anchoring flag), escape-aware literal matching (wildcard metacharacters deliberately literal until their rows land), last-match-wins per source, source precedence deepest .gitignore -> info/exclude -> excludesFile, and the excluded-ancestor walk.
Changed: src/lib.rs rewritten from stub builder to working core. Contract: is_ignored(rel_path, is_dir) signature unchanged; caller (differential bin) untouched. BACKLOG.md row-blank-and-comment line deleted.
Checkpoint: c7c8c158b5e6b224d3247a438973038572ab62ef; PLAN.md row blank-and-comment ticked with this hash in the bookkeeping edit. Stall check: lib.rs changed outside state files, row task closed, no stall.
Verification: Acceptance observed failing first this iteration: slice replay 3 disagreements against the stub (a.txt, note, escaped \#hash), then 0 disagreements after, exit 0, 4 cases 8 queries. cargo test: 5 passed across lib and harness (parse edges, trim port incl tab-not-trimmed, oracle contract pins). Full Verify: exit 1 with 45 disagreements (was 69) - red at last checkpoint too, not a regression; anchoring, directory-only, trailing-whitespace slices went green as core side effects, their rows stay unticked pending their own sweep iterations. cargo tree: gitignore-matcher alone.
Learnings: none.
Next: row-trailing-whitespace - slice already shows 0 disagreements from the trim port; sweep and tick it.

## iter 6/12 | cd1c2080-020939 | 2026-08-04 | row-trailing-whitespace | done

Task: row-trailing-whitespace (High, runtime) - sweep inventory row 2. The slice went green as a side effect of iter 5's trim port, so a bare replay would rubber-stamp; grew the slice from 4 to 8 cases first (two escaped trailing spaces, lone trailing backslash, interior spaces with trailing trim, escaped-space-only pattern vs a single-space path), then swept.
Changed: corpus/trailing-whitespace/05-08.case hand-authored per the NUL token format (od-verified); PLAN.md growth line added under frozen counts; BACKLOG.md row line deleted. No source changes - the existing trim port survived all new cases.
Checkpoint: 2f0a52af22656cdedad5af1704c6334e6891c5a9; PLAN.md row trailing-whitespace ticked with this hash in the bookkeeping edit. Stall check: 4 corpus case files created outside state files, row task closed, no stall.
Verification: Acceptance: failing-first evidence is iter 4's recorded 4 disagreements on this slice against the stub; grown slice replays 8 cases, 18 queries, 0 disagreements, exit 0 - including the genuinely new cases the matcher had never seen. Full Verify: exit 1, 57 cases, 132 queries, 45 disagreements (unchanged set, all in glob-engine slices), red at last checkpoint too, not a regression. Corpus grew 53 to 57 cases, never shrank. cargo test: 4 suites ok. cargo tree: gitignore-matcher alone.
Learnings: Growing a green slice before ticking its row turns a rubber-stamp sweep into a real one; the growth cases are chosen blind to the implementation (oracle decides), so agreement is evidence, not tautology.
Next: row-negation - slice shows 2 disagreements; needs * wildcard support in the glob engine.

## iter 7/12 | cd1c2080-020939 | 2026-08-04 | row-negation | done

Task: row-negation (High, runtime) - sweep inventory row 3. The negation mechanics (last-match-wins, ! flag, \! escape) already agreed; the slice's two disagreements both needed *, so this iteration landed the segment glob engine: * and ? and bracket expressions that never match /, ranges, ] -literal-first, ! and ^ class negation, backslash escapes, lone trailing backslash matches nothing, unclosed class matches nothing. ** stays deliberately non-special until the globstar rows.
Changed: src/lib.rs - lit_match replaced by glob_match plus class_match; pattern_matches now globs. Contract preserved: is_ignored signature and source precedence untouched; swept rows blank-and-comment and trailing-whitespace re-verified green under the new engine in the same full replay, so no flip-back needed. BACKLOG.md row line deleted. 3 new unit tests pin slash-crossing, classes, escapes.
Checkpoint: f949a7904bced6f69ce640ff7c16154f1346f9f9; PLAN.md row negation ticked with this hash in the bookkeeping edit. Stall check: lib.rs changed outside state files, row task closed, no stall.
Verification: Acceptance: observed failing first this iteration - slice replay 2 disagreements (a.log, b.log vs pattern *.log), then 0 after, 4 cases 7 queries, exit 0. Full Verify: 45 disagreements fell to 8, all in globstar-leading and globstar-infix (the un-implemented ** rows); globstar-trailing went green via the excluded-ancestor walk judging abc/d ignored under abc/**, the same mechanism git uses. Red at last checkpoint too, not a regression. cargo test: 4 suites ok (9 tests total). cargo tree: gitignore-matcher alone.
Learnings: none.
Next: row-directory-only - slice already green from the core; grow it with oracle-blind cases before ticking, per the Lessons rule.

## iter 8/12 | cd1c2080-020939 | 2026-08-04 | row-directory-only | done

Task: row-directory-only (High, runtime) - sweep inventory row 4. Slice was green from the core, so grew it 5 to 9 cases first per the Lessons rule: negated dir pattern (build/ then !build/), unanchored cache/ matching a nested dir plus a nonexistent same-name at root, wildcard *.d/ against a real dir and a same-named file, and foo\/ whose body degenerates to a lone backslash.
Changed: corpus/directory-only/06-09.case hand-authored; PLAN.md growth line added; BACKLOG.md row line deleted. No source changes - existing dir_only handling, is_dir stat plumbing, and the ancestor walk survived all new cases.
Checkpoint: 67109f71d6ed2e1fabe443b2b00cdb4ff12dca96; PLAN.md row directory-only ticked with this hash in the bookkeeping edit. Stall check: 4 corpus case files created outside state files, row task closed, no stall.
Verification: Acceptance: failing-first evidence is iter 4's recorded 4 disagreements on this slice against the stub; grown slice replays 9 cases, 17 queries, 0 disagreements, exit 0. The foo\/ prediction (dir_only strip leaves lone-backslash body, matches nothing) confirmed live by the oracle. Full Verify: exit 1, 61 cases, 142 queries, 8 disagreements, all in globstar-leading and globstar-infix; red at last checkpoint too, not a regression. Corpus grew 57 to 61, never shrank. cargo test: 4 suites ok. cargo tree: gitignore-matcher alone.
Learnings: none.
Next: row-anchoring - also green from the core; grow with oracle-blind anchoring cases before ticking.

## iter 9/12 | cd1c2080-020939 | 2026-08-04 | row-anchoring | done

Task: row-anchoring (High, runtime) - sweep inventory row 5. Slice was green from the core, so grew it 4 to 8 cases first: doc/frotz inside sub/.gitignore (anchored to the nested file's directory, not the root and not deeper), anchored /*.txt at root only, a/*/c with the star bound to exactly one segment, and anchored dir-only /build/ not matching sub/build.
Changed: corpus/anchoring/05-08.case hand-authored; PLAN.md growth line added; BACKLOG.md row line deleted. No source changes - the anchoring flag, per-source rel-path computation, and glob engine survived all new cases.
Checkpoint: f94168a6335a112962770b6dcf53e96adcb2a854; PLAN.md row anchoring ticked with this hash in the bookkeeping edit. Stall check: 4 corpus case files created outside state files, row task closed, no stall.
Verification: Acceptance: failing-first evidence is iter 4's recorded 5 disagreements on this slice against the stub; grown slice replays 8 cases, 19 queries, 0 disagreements, exit 0. Full Verify: exit 1, 65 cases, 152 queries, 8 disagreements, all in globstar-leading and globstar-infix; red at last checkpoint too, not a regression. Corpus grew 61 to 65, never shrank. cargo test: 4 suites ok. cargo tree: gitignore-matcher alone.
Learnings: none.
Next: row-wildcards - green from the glob engine; grow with oracle-blind wildcard cases before ticking.

## iter 10/12 | cd1c2080-020939 | 2026-08-04 | row-wildcards | done

Task: row-wildcards (High, runtime) - sweep inventory row 6. Slice was green from the glob engine, so grew it 5 to 9 cases first: bare * (matching dotfiles too - gitignore globs carry no shell dotfile exemption, confirmed by the oracle), a*b*c multi-star backtracking including the abcbc absorb case, ??.txt repeated single-char, and *.[ch] star-plus-class with a basename match at depth.
Changed: corpus/wildcards/06-09.case hand-authored; PLAN.md growth line added; BACKLOG.md row line deleted. No source changes - the glob engine survived all new cases.
Checkpoint: 1db77f8e8b793425caa376e2809d55e0bf0879b3; PLAN.md row wildcards ticked with this hash in the bookkeeping edit. Stall check: 4 corpus case files created outside state files, row task closed, no stall.
Verification: Acceptance: failing-first evidence is iter 4's recorded 8 disagreements on this slice against the stub; grown slice replays 9 cases, 31 queries, 0 disagreements, exit 0. Full Verify: exit 1, 69 cases, 166 queries, 8 disagreements, all in globstar-leading and globstar-infix; red at last checkpoint too, not a regression. Corpus grew 65 to 69, never shrank. cargo test: 4 suites ok. cargo tree: gitignore-matcher alone.
Learnings: none.
Next: row-char-classes - green from the engine; grow with oracle-blind class cases before ticking.

## iter 11/12 | cd1c2080-020939 | 2026-08-04 | row-char-classes | done

Task: row-char-classes (High, runtime) - sweep inventory row 7. Slice was green from the glob engine, so grew it 7 to 12 cases first: [a\-c] escaped hyphen as literal member (not a range - bx must not match), [a-a] single-char range, [a/b] class containing a slash member, [!\!] negated class with escaped bang, [z-a] inverted range matching nothing.
Changed: corpus/char-classes/08-12.case hand-authored; PLAN.md growth line added; BACKLOG.md row line deleted. No source changes - class_match survived all new cases including the inverted range.
Checkpoint: 7912a65e559a1c456ed84cb4807e9cf2bf27ac37; PLAN.md row char-classes ticked with this hash in the bookkeeping edit. Stall check: 5 corpus case files created outside state files, row task closed, no stall.
Verification: Acceptance: failing-first evidence is iter 4's recorded 9 disagreements on this slice against the stub; grown slice replays 12 cases, 30 queries, 0 disagreements, exit 0. Full Verify: exit 1, 74 cases, 178 queries, 8 disagreements, all in globstar-leading and globstar-infix; red at last checkpoint too, not a regression. Corpus grew 69 to 74, never shrank. cargo test: 4 suites ok. cargo tree: gitignore-matcher alone.
Learnings: none.
Next: iteration 12 is the final iteration - WRAPUP: tidy the ledger and write the handoff; the remaining 5 rows (globstar x3 needing ** implementation, precedence, nested-and-layered) carry to the next run.

## iter 12/12 | cd1c2080-020939 | 2026-08-04 | WRAPUP | done

Task: WRAPUP - final iteration; the next row task (row-globstar-leading) requires implementing ** and cannot both land and be swept safely in the last slot, and 4 more rows follow it, so the run closes with a tidy ledger and this handoff instead.
Changed: JOURNAL.md only (this entry); BACKLOG.md already tidy - 5 one-line row tasks in Next, in frozen inventory order, nothing in Now or Later, nothing Proposed, nothing blocked.
Checkpoint: d6a79c65ad3a563a3d1993f3b9c1b5335bc0085f. Stall check: only state files changed this iteration and no backlog item changed state - this is the designed WRAPUP shape on the final iteration, not a stall; the previous primary entry closed a task, so no consecutive no-progress pair exists.
Verification: Final Verify this iteration: exit 1, 74 cases, 178 queries, 8 disagreements, 0 harness failures - the 8 sit entirely in globstar-leading (4) and globstar-infix (4). Slices green but rows unswept: globstar-trailing, precedence, nested-and-layered. Inventory position 7 of 12 rows swept, each ticked with its sweep commit. Corpus grew 53 to 74 cases across the run, never shrank.
Learnings: Handoff for the next run, in ledger order: (1) row-globstar-leading and the ** work - extend glob_match in src/lib.rs; per gitignore(5), ** is special only as a whole path segment (leading **/, trailing /**, infix /**/), and ** adjacent to other characters behaves as a plain *; note **/foo is anchored by its slash, so the fix lives in path-relative matching, and expect the current engine's 8 disagreements to be the failing-first evidence. (2) globstar-trailing, precedence, nested-and-layered are green side-effect slices - grow each with oracle-blind cases before ticking, per the Lessons rule. (3) Invariants that bind every future run: corpus only grows; corpus_gen freeze guard stays; oracle contract in PLAN.md is settled fact; verify command never narrows.
Next: fresh session, relaunch /jeffy in this directory; the state files carry everything forward.

## iter 1/12 | ca25dfca-024109 | 2026-08-04 | row-globstar-leading | done

Task: row-globstar-leading (High, runtime) - sweep inventory row 8 by implementing ** in the glob engine, per the previous run's handoff.
Changed: src/lib.rs - glob_match star arm now consumes the whole star run and crosses / only when a ** run forms a whole path segment, with the **/ zero-directory branch; pattern_matches anchored branch now ports dir.c match_pathname's literal-prefix strip via no_wildcard_len. Contract preserved: is_ignored signature and source precedence untouched; all 7 swept rows re-verified green under the changed engine in the same full replay, so no flip-back needed. corpus/globstar-leading/04-06.case hand-authored (od-verified); PLAN.md growth line added; BACKLOG.md row line deleted. 2 new unit tests pin whole-segment globstar semantics and the prefix-strip quirk.
Checkpoint: 7365453e92a6ea249698ba58a04fedc2abd58e42; PLAN.md row globstar-leading ticked with this hash in the bookkeeping edit. Stall check: src/lib.rs and 3 corpus case files changed outside state files, row task closed, no stall.
Verification: Acceptance observed failing first this iteration: slice replay 4 disagreements against the old engine (foo, a/b/foo, foo/bar, dir-only d - the zero-depth and multi-depth shapes), then 0 after implementing. Oracle-blind growth caught a real defect live: pure wildmatch semantics call x**/foo not-special, but git strips the literal prefix in dir.c match_pathname before wildmatch, so x**/foo ignores x/a/foo and xfoo (2 disagreements, fix attempt 1 landed the strip, then 0). Grown slice: 6 cases, 19 queries, 0 disagreements, exit 0. Full Verify: exit 0, 77 cases, 188 queries, 0 disagreements, 0 harness failures - first green full replay; globstar-infix went green as a side effect, its row stays unticked pending grow-before-tick. Corpus grew 74 to 77, never shrank. cargo test: 9 tests across 4 suites, all ok. cargo tree: gitignore-matcher alone.
Learnings: git's gitignore semantics live in dir.c call sites as much as in wildmatch: the match_pathname literal-prefix strip is semantic, making the first wildcard run segment-initial (x**/foo is x + globstar, not x*/foo). Port the composition, never the manpage alone; the oracle judges the composition.
Next: row-globstar-trailing - slice green from the trailing-/** arm; grow with oracle-blind cases before ticking, per the Lessons rule.

## iter 2/12 | ca25dfca-024109 | 2026-08-04 | row-globstar-trailing | done

Task: row-globstar-trailing (High, runtime) - sweep inventory row 9. Slice green as a side effect of iter 1's ** engine, so grew it 3 to 6 cases first per the Lessons rule, oracle-blind: build/** plus !build/keep (re-inclusion works because dir/** excludes contents, never the dir itself), attached a/x** (the prefix-strip quirk's trailing twin - a/xfoo/bar is ignored because the stripped ** crosses depth, and a/x itself matches the bare ** remainder), and dir-only d/**/ (ignores the subdirectory, spares the file).
Changed: corpus/globstar-trailing/04-06.case hand-authored per the NUL token format (od-verified); PLAN.md growth line added; BACKLOG.md row line deleted. No source changes - the iter 1 engine survived all new cases.
Checkpoint: c68865bae65b0f0beea474fb81bb7e18146c3e60; PLAN.md row globstar-trailing ticked with this hash in the bookkeeping edit. Stall check: 3 corpus case files created outside state files, row task closed, no stall.
Verification: Acceptance: failing-first evidence is iter 4's recorded full-replay failure against the stub, every slice showing disagreements (69 total); grown slice replays 6 cases, 19 queries, 0 disagreements, exit 0 - all three oracle-blind predictions confirmed live by the oracle. Full Verify: exit 0, 80 cases, 198 queries, 0 disagreements, 0 harness failures. Corpus grew 77 to 80, never shrank. cargo test: 9 tests across 4 suites, all ok. cargo tree: gitignore-matcher alone.
Learnings: none.
Next: row-globstar-infix - slice green from the ** engine; grow with oracle-blind cases before ticking.

## iter 3/12 | ca25dfca-024109 | 2026-08-04 | row-globstar-infix | done

Task: row-globstar-infix (High, runtime) - sweep inventory row 10. Slice green as a side effect of iter 1's ** engine, so grew it 3 to 6 cases first per the Lessons rule, oracle-blind: chained a/**/b/**/c (both runs collapse to zero dirs in a/b/c, expand independently in a/x/b/y/c), dir-only infix a/**/tmp/ at zero and one depth plus an out-of-anchor miss, infix inside a nested .gitignore (anchored to sub, root x/a/y unmatched), and attached a/b**/c (prefix strip makes the run segment-initial, so a/b/x/c is ignored, not just a/bx/c).
Changed: corpus/globstar-infix/04-06.case hand-authored per the NUL token format (od-verified); PLAN.md growth line added; BACKLOG.md row line deleted. No source changes - the iter 1 engine survived all new cases.
Checkpoint: 149b1b6985016d9b8dbc857efbd921e9a3b81e62; PLAN.md row globstar-infix ticked with this hash in the bookkeeping edit. Stall check: 3 corpus case files created outside state files, row task closed, no stall.
Verification: Acceptance: failing-first evidence is this run's iter 1 pre-fix full replay showing globstar-infix 4 disagreements against the pre-** engine (recorded in the iter 12/12 WRAPUP of the previous run and re-observed at iter 1 start); grown slice replays 6 cases, 22 queries, 0 disagreements, exit 0. Full Verify: exit 0, 83 cases, 210 queries, 0 disagreements, 0 harness failures. Corpus grew 80 to 83, never shrank. cargo test: 9 tests across 4 suites, all ok. cargo tree: gitignore-matcher alone.
Learnings: none.
Next: row-precedence - slice green from the core's last-match-wins; grow with oracle-blind precedence cases before ticking.

## iter 4/12 | ca25dfca-024109 | 2026-08-04 | row-precedence | done

Task: row-precedence (High, runtime) - sweep inventory row 11. Slice green from the core's last-match-wins, so grew it 4 to 7 cases first per the Lessons rule, oracle-blind. The frozen cases already covered re-exclusion ping-pong and last-matching-vs-last-line, so growth targeted last-wins interacting with the other pattern flags: temp/ then !temp (plain negation last spares the dir at root and at depth), anchored sub/a.txt overridden by a later unanchored !a.txt and the reverse order re-excluding sub/d.txt, and n then !n/ vs f then !f/ (dir-only negation matches the dir, never the file, so n survives and f stays ignored).
Changed: corpus/precedence/05-07.case hand-authored per the NUL token format (od-verified); PLAN.md growth line added; BACKLOG.md row line deleted. No source changes - last_match, the dir_only gate, and the anchoring split survived all new cases.
Checkpoint: 1b32ddc656d40b60dc8c08942b18995450bd58b3; PLAN.md row precedence ticked with this hash in the bookkeeping edit. Stall check: 3 corpus case files created outside state files, row task closed, no stall.
Verification: Acceptance: failing-first evidence is iter 4 of the previous run - full replay against the stub showed every slice failing (69 disagreements); grown slice replays 7 cases, 14 queries, 0 disagreements, exit 0. Full Verify: exit 0, 86 cases, 217 queries, 0 disagreements, 0 harness failures. Corpus grew 83 to 86, never shrank. cargo test: 9 tests across 4 suites, all ok. cargo tree: gitignore-matcher alone.
Learnings: none.
Next: row-nested-and-layered - the last unswept row; grow with oracle-blind cases exercising deeper-overrides-shallower and the source layering beneath the tree.

## iter 5/12 | ca25dfca-024109 | 2026-08-04 | row-nested-and-layered | done

Task: row-nested-and-layered (High, runtime) - sweep inventory row 12, the last unswept row. Slice green from the core's source-precedence stack, so grew it 7 to 10 cases first per the Lessons rule, oracle-blind: !keep.txt inside build/.gitignore while build/ is excluded by the root (the ancestor exclusion is absolute - a negation inside an excluded directory is never honored), tree !keep.dat overriding a core.excludesFile *.dat (the X-to-F pairing; only E-to-F existed), and sub/.gitignore carrying both an own-directory-anchored /local.txt (matches sub/local.txt, not sub/deep/local.txt or root local.txt) and cache/ whose exclusion kills sub/cache/data.txt through the ancestor walk.
Changed: corpus/nested-and-layered/08-10.case hand-authored per the NUL token format (od-verified); PLAN.md growth line added; BACKLOG.md row line deleted. No source changes - the precedence stack, ancestor walk, and per-source rel-path anchoring survived all new cases.
Checkpoint: 449703383cd28c20561e2816f3b6d3bf95779109; PLAN.md row nested-and-layered ticked with this hash in the bookkeeping edit. Stall check: 3 corpus case files created outside state files, row task closed, no stall.
Verification: Acceptance: failing-first evidence is iter 4 of the previous run - full replay against the stub showed every slice failing (69 disagreements); grown slice replays 10 cases, 20 queries, 0 disagreements, exit 0. Full Verify: exit 0, 89 cases, 224 queries, 0 disagreements, 0 harness failures. Corpus grew 86 to 89, never shrank. cargo test: 9 tests across 4 suites, all ok. cargo tree: gitignore-matcher alone.
Learnings: none.
Next: ledger empties with all 12 rows swept - Scoped-mode audit against the Definition of done to derive any remaining tasks or start closeout.

## iter 6/12 | ca25dfca-024109 | 2026-08-04 | AUDIT | audit

Task: Scoped-mode audit against the Definition of done, ledger empty and all 12 rows ticked. Fresh evidence this iteration: cargo build --release exit 0; full Verify exit 0 - 89 cases, 224 queries, 0 disagreements, 0 harness failures, every slice individually 0; cargo test 9 tests across 4 suites ok; cargo tree shows gitignore-matcher alone; 12 rows [x] and 0 unswept in PLAN.md; corpus on disk 89 case files matching the recorded total, monotone growth 53-57-61-65-69-74-77-80-83-86-89 never shrinking; corpus_gen freeze guard re-probed live - refuses the existing corpus with exit 1 and leaves the tree untouched. Staleness handled: the 7 rows ticked in the previous run (blank-and-comment, trailing-whitespace, negation, directory-only, anchoring, wildcards, char-classes) recorded sweep commits predating this run's iter 1 engine change to glob_match and pattern_matches, so they were formally stale; this audit's full replay re-swept all 7 slices at the current commit with 0 disagreements, and the bookkeeping edit re-ticks them with this audit's checkpoint hash. Doc consistency re-checked on the changed module: glob_match, pattern_matches, Pattern, and Matcher doc comments state the implemented semantics including the prefix-strip quirk.
Changed: no source or corpus changes; PLAN.md row hashes refreshed in the bookkeeping edit; BACKLOG.md untouched (nothing filed).
Checkpoint: 18bf16df780d562bc42b37be586d47534f5beff8; the 7 stale rows re-ticked with this hash in the bookkeeping edit (grep confirms 7 rows carry it, 12 rows [x] total). Stall check: audit iteration, no non-state file changed and no backlog item changed state - this is the designed audit shape entering closeout, not a stall; the previous primary entry closed a row task, so no consecutive no-progress pair exists.
Verification: Scores: High 0, Medium 0, Low 0 in-envelope, all 12 inventory rows swept fresh this iteration by the full replay - no unexamined remainder. DoD items 1 through 6 verified true with the evidence above; items 7 and 8 (evaluator PASS and the Converged line) are the convergence sequence itself. Closeout begins: no further audits or replenishment this run; the remaining work is the evaluator gate and, on PASS, the declaration.
Learnings: none.
Next: evaluator gate next iteration - ledger empty, clean full audit recorded, 6 iterations remain, so the gate runs now rather than deferring to a declaration it could reject too late.

## iter 7/12 | ca25dfca-024109 | 2026-08-04 | EVALUATOR | audit

Task: Adversarial evaluator gate, run early per the gate-timing rule (ledger empty, clean audit at iter 6, 5 iterations of budget beyond this one). Fresh-context sub-agent reviewed the run against 6e23baf92042053c50c567d243ad2401341b2085, re-ran the Verify command (exit 0, 89 cases, 224 queries, 0 disagreements), all 5 closed tasks' slice replays (all 0 disagreements), cargo test (9 ok), cargo tree (crate alone), corpus count (89, monotone), and probed beyond the corpus with scratch cases against the live oracle.
Changed: BACKLOG.md gains 3 evaluator-filed Medium tasks in Now (fix-globstar-escaped-slash, fix-posix-bracket-classes, fix-bom-skip). No source changes this iteration.
Checkpoint: d4c0ac2c74f3e60b9562571784977aa38182c3b3. Stall check: 3 backlog items changed state (created open), evaluator iteration, no stall.
Verification: Evaluator: REJECT - three in-envelope Medium disagreements reproduced through the run's own harness on scratch cases (3 cases, 8 queries, 4 disagreements, exit 1): a/**\/b not crossing directories (glob_match segment-end omits wildmatch.c's backslash-slash case), [[:digit:]].txt disagreeing both directions (no POSIX bracket classes in class_match), and an unskipped UTF-8 BOM making the first pattern of an ignore file dead. All three are plausible hand-authored gitignore content, squarely in-envelope, Medium per the rubric (in-envelope case outside the corpus that disagrees). Each filed as an ordinary task at rubric severity; the frozen-corpus Verify remains green this iteration (exit 0 recorded above). Invocation cap: this was invocation 1 of 2 (first landed after the midpoint); the re-invocation at declaration is the last, so all three fixes must land and hold before it.
Learnings: The Lessons rule already says port the composition, not the manpage; the evaluator found the same failure mode a third way (wildmatch.c's escaped-slash clause, POSIX classes, and dir.c's BOM skip all live outside gitignore(5)). The class rule applies: the structural answer is reading wildmatch.c and the dir.c ignore-file reader as the spec, which the three filed fixes complete.
Next: fix-globstar-escaped-slash - grow corpus/globstar-infix with the a/**\/b shape, observe it failing, extend the segment-end test.

## iter 8/12 | ca25dfca-024109 | 2026-08-04 | fix-globstar-escaped-slash | done

Task: fix-globstar-escaped-slash (Medium, runtime), evaluator-filed. glob_match's segment-end test accepted only end-of-pattern or /, omitting wildmatch.c's backslash-slash clause, so a/**\/b degraded to a plain star run and never crossed directories.
Changed: src/lib.rs - at_seg_end gains the backslash-slash case, and the zero-directory branch is guarded to fire only on a literal / (wildmatch grants zero-dir to `**/`, never `**\/`); doc comment updated to state both. Contract preserved: is_ignored signature untouched; the change only widens which star runs cross slashes, and every swept slice re-verified green under the changed engine in the same full replay. corpus/globstar-infix/07.case hand-authored (od-verified); PLAN.md growth line added; BACKLOG.md task line deleted. 3 new asserts pin the escaped-slash semantics.
Checkpoint: eee469021ee289f237b0299b1e2fdd853e33be4b; PLAN.md row globstar-infix re-ticked with this hash (its slice grew and re-swept here); the other rows re-verified green in the same full replay. Stall check: src/lib.rs and 1 corpus case file changed outside state files, task closed, no stall.
Verification: Acceptance observed failing first this iteration: grown slice replayed 1 disagreement (a/x/y/b, oracle=ignored via pattern a/**\/b, matcher=not-ignored; a/x/b agreed by accident because plain-star-plus-literal covers exactly one level, and the oracle confirmed no zero-dir reading at a/b), then 0 disagreements after the fix, 7 cases, 26 queries, exit 0. Fix attempt 1 of 3. Full Verify: exit 0, 90 cases, 228 queries, 0 disagreements, 0 harness failures. Corpus grew 89 to 90, never shrank. cargo test: 9 tests across 4 suites, all ok (escaped-slash asserts added to the globstar test). cargo tree: gitignore-matcher alone.
Learnings: none.
Next: fix-posix-bracket-classes - grow corpus/char-classes with [[:digit:]] cases, observe failing both directions, implement POSIX classes in class_match.

## iter 9/12 | ca25dfca-024109 | 2026-08-04 | fix-posix-bracket-classes | done

Task: fix-posix-bracket-classes (Medium, runtime), evaluator-filed. Implemented POSIX bracket classes in class_match per wildmatch.c, and the failing-first replay forced a larger discovery: the oracle matched ax against [[:upper:][:digit:]]x, which is only explicable by WM_CASEFOLD - git init on this machine sets core.ignoreCase=true in every materialized repo, so the oracle matches case-insensitively everywhere. Probed the oracle live to pin the exact fold semantics: literal K.txt matches k.txt; range ra[m-p]q matches raNq and u[A-C]v matches ubv (two-sided fold); bracket member [K] matches nothing under icase, not even xKy, while [k] matches both cases (members are read outside wildmatch's fold); [[:lower:]] accepts AL. Implemented the full composition: posix_class_matches (12 names, unknown aborts the pattern, missing :] falls back to a literal set), casefold threading through glob_match/class_match/pattern_matches (prefix strip compares via eq_ignore_ascii_case, per strncmp_icase), Matcher::set_ignore_case, and the differential driver mirroring each repo's core.ignorecase into the matcher exactly as dir.c reads it.
Changed: src/lib.rs (class_match POSIX branch, posix_class_matches, icase threading, Matcher field and setter, docs stating the fold rules); src/bin/differential.rs (materialize reads core.ignorecase). Contract note: is_ignored signature unchanged; set_ignore_case is a new public method, documented, caller = differential driver; every swept slice re-verified green in the same full replay. corpus/char-classes/13-15.case and corpus/wildcards/10.case hand-authored; PLAN.md growth and Lessons lines added; BACKLOG.md task line deleted.
Checkpoint: 4e39d2500b95b8b8c7777cfc13aa27846c73eeba; PLAN.md rows char-classes and wildcards re-ticked with this hash (their slices grew and re-swept here); the other rows re-verified green in the same full replay. Stall check: src/lib.rs, src/bin/differential.rs, and 4 corpus case files changed outside state files, task closed, no stall.
Verification: Acceptance observed failing first: char-classes replay showed 7 disagreements pre-fix ([[:digit:]] both directions, upper/digit and negated-space classes, and the icase reveal). Fix attempt 1 (classes plus casefold) left 1: query :w - resolved as a non-finding: check-ignore parses a leading colon as pathspec magic, so the oracle matched the path w, not :w; a colon-leading path is degenerate (illegal on NTFS, CLI-transformed before matching) and the draft query, authored and uncommitted this same iteration, was corrected to [w which genuinely probes the literal-[ fallback member; the committed corpus only ever grew (90 to 94). Attempt 2: char-classes 15 cases, 43 queries, 0 disagreements, exit 0; wildcards 10 cases, 37 queries, 0 disagreements. Full Verify: exit 0, 94 cases, 247 queries, 0 disagreements, 0 harness failures. cargo test: 10 tests across 4 suites, all ok (new posix_classes_and_casefold pins the probed semantics). cargo tree: gitignore-matcher alone.
Learnings: The oracle environment is part of the spec: core.ignoreCase=true on NTFS makes every verdict case-folded, invisible until a cross-case probe. Added to PLAN.md Lessons, with the pathspec-magic colon rule.
Next: fix-bom-skip - grow corpus/blank-and-comment with a BOM-prefixed ignore file, observe failing, skip the BOM in parse_source.

## iter 10/12 | ca25dfca-024109 | 2026-08-04 | fix-bom-skip | done

Task: fix-bom-skip (Medium, runtime), evaluator-filed, the last open ledger item. parse_source now strips a UTF-8 BOM (EF BB BF) at the head of every ignore source before decoding, the one-line port of dir.c add_patterns_from_buffer; a BOM anywhere else stays literal bytes.
Changed: src/lib.rs (parse_source head-BOM strip with doc comment; new unit test pinning head-stripped vs mid-file-literal). Contract preserved: parse_source is private, callers are the three Matcher source setters, and only head-BOM inputs change behavior; every swept slice re-verified green in the same full replay. corpus/blank-and-comment/05.case hand-authored (od-verified: 357 273 277 bytes in place); PLAN.md growth line added; BACKLOG.md task line deleted.
Checkpoint: 7a43e72e3210b23e1bc1065daee0244e91dd8cd2; PLAN.md row blank-and-comment re-ticked with this hash (its slice grew and re-swept here); the other rows re-verified green in the same full replay. Stall check: src/lib.rs and 1 corpus case file changed outside state files, task closed, no stall.
Verification: Acceptance observed failing first this iteration: grown slice replayed 2 disagreements (foo via .gitignore head BOM, sec via info/exclude head BOM - the oracle's own evidence showed both patterns parsed at line 1 with the BOM stripped; mid-file BOM qux agreed as unmatched on both sides), then 0 disagreements after, 5 cases, 11 queries, exit 0. Fix attempt 1 of 3. Full Verify: exit 0, 95 cases, 250 queries, 0 disagreements, 0 harness failures. Corpus grew 94 to 95, never shrank. cargo test: 11 tests across 4 suites, all ok. cargo tree: gitignore-matcher alone.
Learnings: none.
Next: ledger empty, all three evaluator REJECT reasons fixed with corpus-pinned acceptance - re-invoke the evaluator gate (invocation 2 of 2) and declare on PASS.

## iter 11/12 | ca25dfca-024109 | 2026-08-04 | EVALUATOR | blocked

Task: Evaluator gate re-invocation at the declaration (invocation 2 of 2; the first landed after the midpoint, so the cap is 2). The fresh-context evaluator re-verified everything the run claims - Verify exit 0 with 95 cases, 250 queries, 0 disagreements; all 8 closed tasks' slice acceptance replays green; 11 tests ok; cargo tree crate-only; corpus at 95 files, monotone 53 to 95; no corpus special-casing; sampled sweep hashes are real commits - then probed beyond the corpus and reproduced two new in-envelope disagreements through the run's own harness.
Changed: BACKLOG.md gains 2 evaluator-filed Medium tasks in Now (fix-icase-dir-filter, fix-posix-space-set). No source changes this iteration.
Checkpoint: cfe76bc18a0e79f5d6503dc83b1032bd2095e4df. Stall check: 2 backlog items changed state (created open), evaluator iteration, no stall.
Verification: Verify command this iteration, before the gate: exit 0, 95 cases, 250 queries, 0 disagreements, 0 harness failures. Evaluator: REJECT - (1) judge()'s nested-.gitignore applicability filter uses case-sensitive strip_prefix under ignore_case: F Sub/.gitignore with foo.txt, Q sub/foo.txt gives matcher=not-ignored vs oracle=ignored (pattern foo.txt at sub/.gitignore:1), the control Q Sub/foo.txt agreeing; (2) posix_class_matches maps space to is_ascii_whitespace plus VT, admitting VT and FF, but git's sane-ctype isspace is space/tab/newline/CR only: a[[:space:]]b at a\x0bb and a\x0cb gives matcher=ignored vs oracle=not-ignored, the control a b agreeing. Both Medium per the rubric (plausible in-envelope cases outside the corpus that disagree). This is the second REJECT: terminal per the invocation cap - no invocation remains to produce the PASS a declaration requires, so the run ends blocked with both findings filed for the next run.
Learnings: The casefold port must reach every path comparison, not only pattern matching - the source-applicability filter is part of the matching semantics. And wildmatch classifies through git's sane ctype, not libc: its ISSPACE is space/tab/newline/CR. Both are one-command fixes sitting first in Now for the next run.
Next: fresh session, relaunch /jeffy in this directory; the next run opens with fix-icase-dir-filter and fix-posix-space-set, each with a corpus-growth acceptance already specified.

## iter 1/12 | 137ae79e-033353 | 2026-08-04 | fix-icase-dir-filter | done

Task: fix-icase-dir-filter (Medium, runtime), evaluator-filed by the prior run, first of its two REJECT findings. judge()'s applicability filter for nested .gitignore files compared directory prefixes byte-exactly even under ignore_case; new private dir_applies helper folds the prefix comparison (eq_ignore_ascii_case, the same fold the WM_CASEFOLD port uses) when ignore_case is set, exact byte compare otherwise, porting dir.c match_pathname's fspathncmp base check.
Changed: src/lib.rs (dir_applies helper, judge filter rewired through it, ignore_case field doc extended to name the prefix fold, unit test icase_folds_nested_gitignore_dir_prefix pinning folded and case-sensitive behavior); corpus/nested-and-layered/11.case hand-authored; PLAN.md growth and Lessons lines; BACKLOG.md task line deleted. Contract preserved: is_ignored signature unchanged; behavior changes only for ignore_case=true matchers whose nested .gitignore directory differs in case from the query, the exact defect; the case-sensitive default is pinned unchanged by the new test's control and every swept slice re-verified green in the same full replay.
Checkpoint: 0da19fcc2298cddfd28ae483f405f952e8f507f5; PLAN.md row nested-and-layered re-ticked with this hash (its slice grew and re-swept here); the other rows re-verified green in the same full replay. Stall check: src/lib.rs and corpus/nested-and-layered/11.case changed outside state files, task closed, no stall.
Verification: Acceptance observed failing first: slice grown 10 to 11 cases, pre-fix replay 1 disagreement - sub/foo.txt matcher=not-ignored vs oracle=ignored (pattern "foo.txt" at sub/.gitignore:1), controls Sub/foo.txt and sub/bar.txt agreeing. Fix attempt 1 of 3. Post-fix slice replay: 11 cases, 23 queries, 0 disagreements, exit 0. Full Verify: exit 0, 96 cases, 253 queries, 0 disagreements, 0 harness failures. Corpus grew 95 to 96, never shrank. cargo test: 12 tests across 4 suites, all ok. cargo tree: gitignore-matcher alone. No batteries under .jeffy/probes (directory absent).
Learnings: WM_CASEFOLD reaches source applicability, not only wildmatch: the nested-.gitignore directory-prefix test folds via fspathncmp. Added to PLAN.md Lessons.
Next: fix-posix-space-set - grow corpus/char-classes with a[[:space:]]b queried at VT and FF, observe failing first, restrict the posix space class to git's sane-ctype set (space, tab, newline, CR).

## iter 2/12 | 137ae79e-033353 | 2026-08-04 | fix-posix-space-set | done

Task: fix-posix-space-set (Medium, runtime), evaluator-filed by the prior run, second of its two REJECT findings, the last open ledger item. posix_class_matches mapped [[:space:]] to is_ascii_whitespace plus VT, admitting VT and FF; git classifies through its own sane-ctype where GIT_SPACE is exactly space, tab, LF, CR, so the space arm now matches exactly those four bytes, with a comment naming the sane-ctype source.
Changed: src/lib.rs (space arm of posix_class_matches; posix_classes_and_casefold test extended to pin all four GIT_SPACE members matching and VT and FF dead). Contract preserved: posix_class_matches is private, callers are class_match alone; only VT and FF inputs to [[:space:]] change verdict, the exact defect; every swept slice re-verified green in the same full replay. corpus/char-classes/16.case hand-authored; PLAN.md growth line added; BACKLOG.md task line deleted, ledger now empty.
Checkpoint: a9724c236a660b859fb90111f1220bf6707eb3f4; PLAN.md row char-classes re-ticked with this hash (its slice grew and re-swept here); the other rows re-verified green in the same full replay. Stall check: src/lib.rs and corpus/char-classes/16.case changed outside state files, task closed, no stall.
Verification: Acceptance observed failing first: slice grown 15 to 16 cases, pre-fix replay 2 disagreements - a VT b and a FF b matcher=ignored vs oracle=not-ignored (no pattern matched), space and tab controls agreeing. Fix attempt 1 of 3. Post-fix slice replay: 16 cases, 47 queries, 0 disagreements. Full Verify: exit 0, 97 cases, 257 queries, 0 disagreements, 0 harness failures. Corpus grew 96 to 97, never shrank. cargo test: 12 tests across 4 suites, all ok. cargo tree: gitignore-matcher alone. No batteries under .jeffy/probes (directory absent).
Learnings: none new; the sane-ctype rule was recorded in PLAN.md Lessons by the prior run's evaluator entry lesson and is now pinned in code and test.
Next: ledger empty with no audit yet this run - iteration 3 runs the Scoped-mode fresh-evidence audit against the Definition of done; on zero High and Medium it opens closeout and the evaluator gate follows with 9 iterations of budget left.

## iter 3/12 | 137ae79e-033353 | 2026-08-04 | AUDIT | audit

Task: Scoped-mode fresh-evidence audit against the Definition of done, the run's first (ledger emptied at iter 2). All 12 slices replayed green this iteration, which re-executes every row's defining sweep check at the current code; no row is stale. Beyond the corpus, 7 oracle-blind scratch probe cases (18 queries) were replayed differentially through the harness, targeting the class the prior run's evaluator exploited: escaped wildcards (\*.txt, \?q), bare ** matching everything, multi-byte UTF-8 (? must not match the 2-byte e-acute; literal cafe-acute byte equality; ASCII-e control dead), anchored /Sub/Foo.txt hit by sub/foo.txt under icase, Dir/** hit by dir/x under icase, degenerate patterns / and ! alone, unclosed [abc matching nothing including its own literal text. 0 disagreements; probes live in the session scratchpad, the frozen corpus untouched.
Changed: no source and no ledger changes; BACKLOG.md remains empty in Now, Next, Later.
Checkpoint: 2128b1ee026a18943f5179a3eb9bd9217a6c08e9. Stall check: this audit iteration changed only JOURNAL.md and no backlog item changed state (nothing filed - the audit scored clean); the previous primary entry closed a task, so no stall.
Verification: Fresh evidence at ce6ebcd: cargo build --release exit 0. Verify command exit 0: 97 cases, 257 queries, 0 disagreements, 0 harness failures. Probe replay exit 0: 7 cases, 18 queries, 0 disagreements. cargo test: 12 tests across 4 suites, all ok. cargo tree: gitignore-matcher alone. Corpus 97 cases, monotone from 53, never shrank. All 12 inventory rows [x] with sweep hashes. DoD items 1-6 verifiably true; items 7-8 are the gate and declaration. Scores: High 0, Medium 0, Low 0 - nothing filed; the scores cover all 12 rows, all swept. Closeout begins: no further audit or replenishment this run.
Learnings: none.
Next: iteration 4 runs the evaluator gate (invocation 1, cap 3 since this lands before the midpoint) and on PASS declares in the same iteration per the closing rule.

## iter 4/12 | 137ae79e-033353 | 2026-08-04 | EVALUATOR | audit

Task: Evaluator gate, invocation 1 of 3 (first landing before the budget midpoint). The fresh-context evaluator re-derived every claim - Verify exit 0 with 97 cases, 257 queries, 0 disagreements; both closed tasks' slice acceptance replays green (nested-and-layered 11 cases 23 queries, char-classes 16 cases 47 queries); 12 tests ok; cargo tree crate-only; corpus file count 97 matching PLAN.md per-slice exactly, monotone 95 to 97; od-verified both new case files exercise what the run claims; judged dir_applies and the space arm faithful to dir.c fspathncmp and sane-ctype GIT_SPACE - then probed 7 oracle-blind cases and reproduced one new in-envelope disagreement through the run's own harness.
Changed: BACKLOG.md gains 1 evaluator-filed Medium task in Now (fix-gitignore-name-case). No source changes this iteration.
Checkpoint: f6bc9b5cdf465f7852aaacbe7e4674b2936813f1. Stall check: 1 backlog item changed state (created open), evaluator iteration, no stall.
Verification: Verify command this iteration, before the gate: exit 0, 97 cases, 257 queries, 0 disagreements, 0 harness failures. Evaluator: REJECT - materialize() in src/bin/differential.rs registers a .gitignore source only when the F path byte-exactly ends in .gitignore, but the oracle discovers ignore files by NTFS case-insensitive name lookup: F Sub/.GitIgnore with foo.txt gives DISAGREE q=sub/foo.txt and q=Sub/foo.txt, matcher=not-ignored vs oracle=ignored (pattern foo.txt at sub/.gitignore:1), probe replay exit 1. Medium per the rubric; the evaluator's other 6 probes (deep-icase dirs, icase precedence, anchored-nested icase, negated space class, LF and CR members, excluded-ancestor walk under icase) all agreed. Not terminal: 2 invocations remain.
Learnings: The icase-reach class has a third site: source-file name recognition in the harness, not only pattern text and dir prefixes. The fix must close the class with the full comparison-site enumeration.
Next: fix-gitignore-name-case, then re-invoke the gate (invocation 2 of 3) and declare on PASS.

## iter 5/12 | 137ae79e-033353 | 2026-08-04 | fix-gitignore-name-case | done

Task: fix-gitignore-name-case (Medium, test), evaluator-filed, the only open ledger item. materialize() in the differential driver recognized an ignore source only by byte-exact .gitignore suffix; git asks the filesystem for <dir>/.gitignore and on an ignore_case repo that lookup folds, so a file authored as .GitIgnore is a live source the matcher never saw. The F-record handler now splits the path at the last slash and accepts the basename case-insensitively when the repo's core.ignorecase is true, byte-exactly otherwise. Third instance of the icase-reach class, closed class-complete: the enumeration of byte comparisons on the verdict path is (1) pattern text vs path in wildmatch (WM_CASEFOLD port: glob_match, class_match, literal prefix strip), (2) nested-.gitignore dir prefix vs query in dir_applies, (3) ignore-source name recognition in materialize - each folded and each pinned by corpus cases the Verify command replays (wildcards/10, char-classes/13-15, nested-and-layered/11-12) plus unit tests; the remaining byte-exact compare, the oracle echo check, is correct as-is because git echoes queries verbatim. Class recorded under Settled classes in BACKLOG.md.
Changed: src/bin/differential.rs (F-record source recognition, materialize doc); corpus/nested-and-layered/12.case hand-authored; PLAN.md growth line and Lessons extension; BACKLOG.md task line deleted, Settled classes line added, ledger empty again. Contract preserved: parse_case grammar, Matcher API, and exit-code semantics unchanged; only which F records reach add_gitignore changes, and only on ignore_case repos for case-variant .gitignore names, the exact defect.
Checkpoint: 22b309a5746d0d6031016b4587a06825fce43210; PLAN.md row nested-and-layered re-ticked with this hash (its slice grew and re-swept here); the other rows re-verified green in the same full replay. Stall check: src/bin/differential.rs and corpus/nested-and-layered/12.case changed outside state files, task closed, no stall.
Verification: Acceptance observed failing first: slice grown 11 to 12 cases, pre-fix replay 2 disagreements - q=sub/foo.txt and q=Sub/foo.txt matcher=not-ignored vs oracle=ignored (pattern foo.txt at sub/.gitignore:1 and Sub/.gitignore:1), control sub/bar.txt agreeing. Fix attempt 1 of 3. Post-fix slice replay: 12 cases, 26 queries, 0 disagreements, exit 0. Full Verify: exit 0, 98 cases, 260 queries, 0 disagreements, 0 harness failures. Corpus grew 97 to 98, never shrank. cargo test: 12 tests across 4 suites, all ok. cargo tree: gitignore-matcher alone. No batteries under .jeffy/probes (directory absent).
Learnings: Source discovery is a filesystem lookup, so filesystem case-insensitivity makes case-variant ignore-file names live sources; recorded in PLAN.md Lessons as an extension of the icase-reach line.
Next: ledger empty, closeout audit already recorded at iter 3, evaluator REJECT reason fixed with corpus-pinned acceptance - iteration 6 re-invokes the gate (invocation 2 of 3) and declares on PASS.

## iter 6/12 | 137ae79e-033353 | 2026-08-05 | EVALUATOR | blocked

Task: Evaluator gate re-invocation at the declaration (invocation 2; first landed before the midpoint so the cap is 3, but a second REJECT is terminal regardless). The fresh-context evaluator re-verified everything the run claims - Verify exit 0 with 98 cases, 260 queries, 0 disagreements; both slice acceptance replays green (nested-and-layered 12 cases 26 queries, char-classes 16 cases 47 queries); 12 tests ok; cargo tree crate-only; corpus count 98 matching PLAN.md, monotone 53 to 98; od-verified the three new case files - then probed beyond the corpus and returned REJECT with three reasons.
Changed: BACKLOG.md gains 2 Medium tasks in Now (fix-source-collision-model, fix-dotslash-query) and the icase-reach Settled line now names the open fourth site. No source changes this iteration.
Checkpoint: 92466b4adb0e0228b6c66dd50d97b58bb3550e3d. Stall check: 2 backlog items changed state (created open), evaluator iteration, no stall.
Verification: Verify command this iteration, before the gate: exit 0, 98 cases, 260 queries, 0 disagreements, 0 harness failures. Evaluator: REJECT - (1) colliding F records (.gitignore then .GITIGNORE) both register as matcher sources while the NTFS write folds to one file, matcher=ignored vs oracle=not-ignored on the first record's pattern; (2) a ./-prefixed query misses an anchored pattern, matcher=not-ignored vs oracle=ignored (pattern /a.log at .gitignore:1); (3) a backslash-separated query claim. Independent re-verification this iteration: (1) and (2) reproduced exactly through the harness (scratch probes, corpus untouched; 2 disagreements with git's evidence attached); (3) refuted by direct oracle evidence - printf 'sub\a.log\0' into check-ignore -z -v --non-matching --no-index --stdin returns three empty fields and exit 1, not ignored, and the differential probe agrees on both sides, so stdin pathspecs are not backslash-folded and no disagreement exists; (3) is not filed. Terminal: second REJECT this run - the run ends blocked with both confirmed findings filed for the next run.
Learnings: The evaluator is bound by the evidence rule and so is the acceptance of its verdict: one of three REJECT reasons did not survive independent reproduction, and the right response is to file what reproduces and record the refutation, not to file all three. The confirmed miss generalizes the icase lesson once more: the filesystem itself is a fold site - materialization must model name collisions on write, not only name recognition on read.
Next: fresh session, relaunch /jeffy in this directory; the next run opens with fix-source-collision-model and fix-dotslash-query, both with reproduced failing cases and acceptance specified.

## iter 1/12 | eaf91400-040822 | 2026-08-05 | fix-source-collision-model | done

Task: fix-source-collision-model (Medium, test), top ledger item, evaluator-filed in run 2. materialize() registered every .gitignore-named F record as a matcher source, but the disk write folds colliding paths - case-variants on an ignore_case filesystem, or the same path recorded twice anywhere - into one file holding the last content under the first name, so the matcher modeled sources the disk cannot hold. The fix models the fold on write: sources are collected keyed by the folded full path (ascii-lowercased when core.ignorecase, byte-exact otherwise), a colliding record replaces the earlier source's content, and registration happens once per surviving file. Keying by the full path, not the basename, folds directory-component collisions (SUB/.GitIgnore onto sub/.gitignore) the same way. Fourth and final site of the icase-reach class; the Settled classes line regains class-complete standing with the full four-site enumeration.
Changed: src/bin/differential.rs (materialize collects sources with fold-on-write replacement, doc extended, new unit test colliding_source_records_fold_to_last_content); corpus/nested-and-layered/13.case hand-authored (root and nested-dir collision pairs, 5 queries); PLAN.md growth line and one Lessons line; BACKLOG.md task line deleted and icase-reach Settled line restored to class-complete. Contract preserved: parse_case grammar, Matcher API, exit-code semantics, and registration order for non-colliding corpora all unchanged - sources register in first-occurrence order, which equals record order when nothing collides, the exact prior behavior.
Checkpoint: d21d01a082e29ad17195b83a84c33978df2f7d75; PLAN.md row nested-and-layered re-ticked with this hash (its slice grew and re-swept here); all other rows re-verified green in the same full replay. Stall check: src/bin/differential.rs and corpus/nested-and-layered/13.case changed outside state files, task closed, no stall.
Verification: Acceptance observed failing first: slice replay pre-fix exit 1 with 2 disagreements - q="alpha.txt" and q="sub/x.txt" matcher=ignored vs oracle=not-ignored (no pattern matched), controls beta.txt, sub/y.txt, gamma.txt agreeing - and the new unit test failing at the first assert. Fix attempt 1 of 3. Post-fix slice replay: 13 cases, 31 queries, 0 disagreements, exit 0. Full Verify: exit 0, 99 cases, 265 queries, 0 disagreements, 0 harness failures. Corpus grew 98 to 99, never shrank. cargo test: 13 tests across 4 suites, all ok. cargo tree: gitignore-matcher alone. No batteries under .jeffy/probes (directory absent).
Learnings: The filesystem folds colliding names on write, not only lookups on read - a harness that materializes authored records must model what survives on disk, never what was authored; recorded in PLAN.md Lessons as a corpus-authoring rule.
Next: fix-dotslash-query, the last open ledger item, then the ledger is empty; closeout audit standing from run 2 does not carry across runs, so this run still needs its own full audit before any evaluator gate.

## iter 2/12 | eaf91400-040822 | 2026-08-05 | fix-dotslash-query | done

Task: fix-dotslash-query (Medium, runtime), the last run-2 evaluator finding. check-ignore normalizes each pathspec before matching - a leading ./ segment, repeated any number of times, is stripped by git's pathspec machinery - while the -z output echoes the pathspec verbatim, so the driver fed raw queries to a matcher that models only the post-normalization world. The fix adds normalize_query in the driver, stripping leading ./ segments before is_ignored and the is_dir metadata lookup; the oracle keeps receiving the original bytes, whose verbatim echo the harness still checks byte-exactly. Contract preserved: Matcher API untouched, oracle invocation and echo check unchanged, and queries without a leading ./ pass through normalize_query byte-identical, so every previously-agreeing corpus case sees the exact prior behavior.
Changed: src/bin/differential.rs (normalize_query helper, call site in the comparison loop, unit test normalize_query_strips_leading_dotslash_only); corpus/anchoring/09.case hand-authored (./ and ././ queries against anchored /a.log, plain and unmatched controls); PLAN.md growth line and one Lessons line; BACKLOG.md task line deleted, one discovered Medium filed (fix-pathspec-normalization-port). Discovery: a scratch probe confirmed check-ignore also collapses interior ./ and resolves .. before matching (sub/./x.txt and sub/../a.log both ignored by anchored patterns, echoed verbatim, oracle exit 0), the second instance of the unnormalized-query root cause, filed as one structural task rather than another instance patch.
Checkpoint: 74fcc8bcaceb19c1fca5ab651f0ee7299b209ee0; PLAN.md row anchoring re-ticked with this hash (its slice grew and re-swept here); all other rows re-verified green in the same full replay. Stall check: src/bin/differential.rs and corpus/anchoring/09.case changed outside state files, task closed, no stall.
Verification: Acceptance observed failing first: slice replay pre-fix exit 1 with 2 disagreements - q="./a.log" and q="././a.log" matcher=not-ignored vs oracle=ignored (pattern "/a.log" at .gitignore:1), controls a.log and b.log agreeing. Fix attempt 1 of 3. Post-fix slice replay: 9 cases, 23 queries, 0 disagreements, exit 0. Full Verify: exit 0, 100 cases, 269 queries, 0 disagreements, 0 harness failures. Corpus grew 99 to 100, never shrank. cargo test: 14 tests across 4 suites, all ok. cargo tree: gitignore-matcher alone. No batteries under .jeffy/probes (directory absent).
Learnings: check-ignore matches the normalized pathspec but echoes it verbatim, so normalization belongs on the matcher side of the comparison only; recorded in PLAN.md Lessons.
Next: fix-pathspec-normalization-port, the only open ledger item; after it the ledger empties and this run still owes its own full audit before any evaluator gate.

## iter 3/12 | eaf91400-040822 | 2026-08-05 | fix-pathspec-normalization-port | done

Task: fix-pathspec-normalization-port (Medium, runtime), the structural completion of the unnormalized-query root cause. Probes settled the port's shape before code: the oracle reported sub//x.txt ignored by /sub/x.txt and nonexist/../a.log ignored by /a.log, so duplicate slashes collapse and .. resolves textually with no existence check, while keep/ echoed with its trailing slash intact. normalize_query now runs git's full textual pass - . segments and duplicate slashes drop, .. consumes the previous segment, one trailing slash survives - and returns unresolvable shapes unchanged (leading /, .. above the root, a query normalizing to nothing) for the oracle to reject as exit 128, keeping harness failures harness failures. Contract preserved: a corpus-wide scan of all 269 pre-existing queries found only iter-2's two ./-cases carrying any normalization-sensitive shape, and those normalize to the same result under the old strip and the new pass, so the port is byte-identity on every previously-agreeing query; the oracle feed and echo check are untouched. The unit test pinning the leading-only strip pinned the exact narrowness this task fixes and is replaced by normalize_query_ports_pathspec_normalization in the same change.
Changed: src/bin/differential.rs (normalize_query full port with doc, call site borrows, replaced unit test); corpus/anchoring/10.case hand-authored (interior ./, .. through existing and nonexistent segments, duplicate slash, matched and unmatched controls); PLAN.md growth line and one Lessons line; BACKLOG.md task line deleted, ledger now empty.
Checkpoint: d705e3fac5d73cd7d7cdc62bb4d39d4c1d870654; PLAN.md row anchoring re-ticked with this hash (its slice grew and re-swept here); all other rows re-verified green in the same full replay. Stall check: src/bin/differential.rs and corpus/anchoring/10.case changed outside state files, task closed, no stall.
Verification: Acceptance observed failing first: slice replay pre-fix exit 1 with 4 disagreements - q="sub/./x.txt" and q="sub//x.txt" vs pattern "/sub/x.txt" at .gitignore:1, q="sub/../a.log" and q="nonexist/../a.log" vs pattern "/a.log" at .gitignore:2, all matcher=not-ignored vs oracle=ignored, controls sub/x.txt and b.log agreeing. Fix attempt 1 of 3. Post-fix slice replay: 10 cases, 29 queries, 0 disagreements, exit 0. Full Verify: exit 0, 101 cases, 275 queries, 0 disagreements, 0 harness failures. Corpus grew 100 to 101, never shrank. cargo test: 14 tests across 4 suites, all ok. cargo tree: gitignore-matcher alone. No batteries under .jeffy/probes (directory absent).
Learnings: Pathspec normalization is textual and existence-blind; recorded in PLAN.md Lessons with the corpus-authoring rule for the shapes git cannot normalize.
Next: ledger empty and this run has no audit yet - iteration 4 runs the full fresh-evidence audit per the Method, sweeping stale rows if any; the evaluator gate only after a clean audit, per the closing rule.

## iter 4/12 | eaf91400-040822 | 2026-08-05 | AUDIT | audit

Task: Full fresh-evidence audit for this run (Scoped mode: remaining work derived from the Definition of done). Scope: all 12 inventory rows replayed fresh through the Verify command this iteration, plus 9 oracle-blind probe families the corpus has never carried, authored in scratch and driven through the harness end-to-end: CRLF-terminated .gitignore lines, trailing-slash query on a directory, trailing-slash query on a file, bare ** pattern, unterminated bracket class alone and after a literal, degenerate bare ! and bare / patterns, non-ASCII pattern under core.ignoreCase with exact and cross-case queries, escaped star matching a literal *.txt name, and dir-only */ at depth one. Probe run: 9 cases, 21 queries, 0 disagreements, 0 harness failures - the trailing-slash-on-file pathspec is accepted by the oracle and both sides agree. Scores: High 0, Medium 0, Low 0 filed - no finding of any severity surfaced by replay, probes, tests, or count reconciliation. Closeout begins with this entry: no further audit or replenishment this run; the remaining work is the convergence sequence alone.
Changed: no source, corpus, or ledger changes; probes live in scratch only. DoD position after evidence: build green; Verify green with output recorded below; 12 of 12 rows ticked with sweep hashes and per-slice disk counts matching every row exactly (enumeration: the reconciliation script compares disk case and query counts per slice to the inventory rows, all 12 OK); cargo tree crate-only; corpus 101 cases 275 queries, monotone over every earlier size this run (99, 100, 101); BACKLOG zero open in Now, Next, Later, Proposed empty, Declined empty; evaluator PASS and Converged line outstanding.
Checkpoint: 8612beae83946344131000e2500e5e01650af0e9. Stall check: no file outside the state files changed and no ledger item changed state this iteration - stated per the rule; this is an audit iteration recording fresh evidence, and the previous primary entry closed a task, so no stall.
Verification: Verify command: exit 0, 101 cases, 275 queries, 0 disagreements, 0 harness failures, per-slice counts matching PLAN rows (12 of 12 OK). cargo test: 14 tests across 4 suites, all ok. cargo tree: gitignore-matcher alone. Probe corpus: exit 0, 9 cases, 21 queries, 0 disagreements, 0 harness failures.
Learnings: Nine fresh probe families agreeing on the first try is the run's three normalization-and-collision fixes paying out; the harness's model of the oracle frontend (pathspec normalization, source discovery, write folding) is now faithful enough that new shapes agree by construction rather than by luck.
Next: ledger empty, clean audit recorded, 8 iterations remain - iteration 5 runs the evaluator gate per the run-the-gate-early rule and declares in the same iteration on PASS.

## iter 5/12 | eaf91400-040822 | 2026-08-05 | EVALUATOR | audit

Task: Evaluator gate, invocation 1 of 3 (landing at iteration 5, before the budget midpoint of 6). The fresh-context evaluator re-derived every claim - Verify exit 0 with 101 cases, 275 queries, 0 disagreements; both closed-task slice replays green (nested-and-layered 13 cases 31 queries, anchoring 10 cases 29 queries); 14 tests ok; cargo tree crate-only; diff review against 27273df found corpus monotone 98 to 101 with additions only and no new dependencies - then authored 8 oracle-blind probe cases (36 queries) and returned REJECT with one reason: normalize_query keeps a trailing slash only when the raw query ends with /, but git's textual normalization also yields dir/ when the final segment is . or .., so q=ghost/. and q=ghost/x/.. miss dir-only pattern ghost/ against a nonexistent directory. Independent re-verification this iteration: reproduced exactly through the harness with my own scratch case - 1 case, 7 queries, 2 disagreements, matcher=not-ignored vs oracle=ignored (pattern ghost/ at .gitignore:1) on both shapes, all 5 controls agreeing including ghost/./ and ghost/x/../ whose raw trailing slash survives the port. In-envelope Medium in code this run wrote; it also narrows fix-pathspec-normalization-port's full-port claim - the port missed the final-dot-segment slash rule. The evaluator's other 34 probe queries agreed.
Changed: BACKLOG.md gains 1 evaluator-filed Medium task in Now (fix-trailing-dot-normalization). No source changes this iteration.
Checkpoint: 9c42e0907deb632ebb58f8bf2f67cc673f45c077. Stall check: 1 backlog item changed state (created open), evaluator iteration, no stall.
Verification: Verify command this iteration, before the gate: exit 0, 101 cases, 275 queries, 0 disagreements, 0 harness failures. Evaluator: REJECT, one substantiated reason, filed at Medium. Not terminal: 2 invocations remain.
Learnings: git normalize_path_copy terminates with a slash when the final segment is . or .. - the dir-ness of a pathspec survives normalization through a trailing dot segment, not only through a literal trailing slash.
Next: iteration 6 fixes fix-trailing-dot-normalization with its acceptance check, then re-invokes the gate (invocation 2) and declares on PASS in the same iteration under the one-transaction rule.

## iter 6/12 | eaf91400-040822 | 2026-08-05 | EVALUATOR | blocked

Task: Declaring iteration under the one-transaction rule: fixed the gate-filed fix-trailing-dot-normalization (Medium, runtime) with its acceptance observed failing first, then re-invoked the evaluator gate (invocation 2; first landed before the midpoint so the cap is 3, but a second REJECT is terminal regardless). The fix: normalize_query now emits the trailing slash git leaves when a pathspec's final segment is . or .. - dir-ness survives the collapse textually - pinned by corpus/directory-only/10.case and four new unit-test shapes including the x/.. passthrough. The fresh-context evaluator re-derived every claim - Verify exit 0 with 102 cases, 280 queries, 0 disagreements; all four closed tasks' slice acceptances reproduced with byte-level corpus verification; 14 tests ok; cargo tree crate-only; corpus monotone 98 to 102 - judged the trailing-dot fix faithful across 42 agreeing probe queries including the discriminating wildcard-tail dir-only shapes, then composed collision probes with non-ASCII case-variants and returned REJECT with two reasons.
Changed: src/bin/differential.rs (normalize_query final-dot rule, doc, four unit-test shapes); corpus/directory-only/10.case hand-authored; PLAN.md growth line, one Lessons line added then narrowed per the evaluator's Low note (wildcard-tail dir-only patterns do consult existence at the path-itself match, literal ones do not); BACKLOG.md fix-trailing-dot-normalization deleted, 2 evaluator-filed Medium tasks added (fix-unicode-collision-fold, fix-unicode-source-applicability), icase-reach Settled line reopened at the Unicode boundary.
Checkpoint: 144b9a0b8eb17375293b2e0dad8d72dde4fe7ebb; PLAN.md row directory-only re-ticked with this hash (its slice grew and re-swept here); all other rows re-verified green in the same full replay. Stall check: src/bin/differential.rs and corpus/directory-only/10.case changed outside state files, 3 ledger items changed state, no stall.
Verification: Acceptance observed failing first: directory-only replay pre-fix exit 1 with 2 disagreements - q="ghost/." and q="ghost/x/.." matcher=not-ignored vs oracle=ignored (pattern "ghost/" at .gitignore:1), controls ghost, ghost/, other.txt agreeing. Fix attempt 1 of 3. Post-fix: slice 10 cases, 22 queries, 0 disagreements; full Verify exit 0, 102 cases, 280 queries, 0 disagreements, 0 harness failures; corpus grew 101 to 102; cargo test 14 across 4 suites all ok; cargo tree crate-only; the run-5 evaluator's 7-query repro case replays 0 disagreements. Evaluator: REJECT, second this run, terminal - (1) the materialize collision fold key is to_ascii_lowercase while NTFS folds Unicode, so colliding sÜb/.gitignore and süb/.gitignore leave one file on disk and two phantom sources in the model; (2) dir_applies and source discovery fold ASCII-only while git reaches nested sources through the filesystem's Unicode fold. Independent re-verification this iteration: both reproduced exactly through my own scratch cases - 2 cases, 4 queries, 3 disagreements matching the evaluator's output byte for byte, git's evidence attached, including the source named in the query's own spelling. Both filed at Medium.
Learnings: The ASCII fold was a conscious iteration-1 choice made to match the existing convention without probing the filesystem's true fold; the filesystem folds Unicode, and every fold this project ports must be sized by direct oracle probes, never by convention. The evaluator gate caught what four iterations of green replays could not, twice.
Next: hard blocker per the second-REJECT rule - the loop ends here. The next run opens with fix-unicode-collision-fold and fix-unicode-source-applicability, both with reproduced failing cases, acceptance specified, and the fold-breadth probe as the first step.

## iter 1/12 | 09db09fe-045619 | 2026-08-05 | fix-unicode-collision-fold | done

Task: fix-unicode-collision-fold (Medium, test, harness) - the materialize collision key was to_ascii_lowercase while the filesystem folds by the volume's own table. First step per the ledger: fold-breadth probe, 11 case pairs driven directly against the oracle, each observed on the write side (do two case-variant .gitignore dirs collide into one file) and the read side (does a query in the other spelling reach the source). Result: the fold is the NTFS $UpCase table, matching no portable fold - ascii, u-diaeresis (U+00FC/U+00DC), fullwidth (U+FF41/U+FF21), and sigma (U+03C3/U+03A3) pairs fold; sharp-s (U+00DF/U+1E9E), dotless-i (U+0131/I), dotted-I (U+0130/i), Kelvin (U+212A/k), final-sigma (U+03C2/U+03A3), Deseret supplementary-plane, and NFC/NFD e-acute pairs stay distinct; on every pair the oracle's verdicts tracked the disk state exactly, both sides. Fix: materialize keys each source by its canonical on-disk path (fs::canonicalize after the write, taken relative to the canonical repo root), so the filesystem answers its own fold; the source's dir spelling comes from the same canonical path and the ASCII-lowercase prediction is gone.
Changed: src/bin/differential.rs (canonical-path collision keying in materialize, doc comments, new unit test unicode_collision_model_mirrors_disk pinning model-mirrors-disk behaviour on both folding and non-folding volumes); corpus/nested-and-layered/14.case hand-authored (folding u-diaeresis collision beside a non-folding sharp-s pair as an anti-overfold control, 6 queries); PLAN.md growth line and one Lessons line; BACKLOG.md fix-unicode-collision-fold deleted, fold-breadth probe result recorded on the surviving task, Settled-classes mechanism phrase updated to canonical-path keying.
Checkpoint: e32a1fe91287c58a6165ff0af3434992f5683f91; PLAN.md row nested-and-layered re-ticked with this hash (its slice grew and re-swept green in the same full replay). Stall check: src/bin/differential.rs and corpus/nested-and-layered/14.case changed outside the state files and 1 ledger item changed state, no stall.
Verification: Acceptance observed failing first: slice replay pre-fix exit 1, 14 cases, 37 queries, 2 disagreements - q="sUb/a.txt" (U+00DC spelling) matcher=ignored vs oracle=not-ignored (no pattern matched), q="sUb/b.txt" matcher=not-ignored vs oracle=ignored (pattern "b.txt" at the U+00DC-spelled source line 1); sharp-s controls agreed pre-fix. Fix attempt 1 of 3. Post-fix: slice 14 cases, 37 queries, 0 disagreements; full Verify exit 0 with output redirected to a file and the real exit status read, 103 cases, 286 queries, 0 disagreements, 0 harness failures; corpus grew 102 to 103, monotone; cargo test 15 tests across 4 suites all ok (was 14, one added); cargo tree crate-only.
Learnings: The volume's fold is data, not an algorithm - $UpCase is fixed at format time and matches no portable Unicode fold, so a faithful collision model reads the disk back (canonicalize) instead of predicting; recorded in PLAN Lessons. The probe also confirmed the read-side lookup folds by the same table, which sizes fix-unicode-source-applicability: dir_applies cannot use any static fold either.
Next: fix-unicode-source-applicability - make source applicability follow the filesystem's fold for queries spelled differently than the materialized source; the probe table and the canonical-spelling sources from this fix are its groundwork.

## iter 2/12 | 09db09fe-045619 | 2026-08-05 | fix-unicode-source-applicability | done

Task: fix-unicode-source-applicability (Medium, runtime, matcher) - dir_applies and the harness's source discovery folded ASCII-only while git reaches nested sources through the volume's own fold. Design probe first: with the disk directory spelled U+00DC and a root .gitignore pattern spelled U+00FC, the oracle matched the byte-equal query and missed the disk-folded spelling - so pattern matching runs against the query's own spelling with the ASCII fold, and only source discovery goes through the filesystem; rewriting query spellings would be wrong, and the fix belongs in applicability. Fix: Matcher gains set_repo_root; with a root set, dir_applies resolves the query prefix at the source's depth and the source dir through fs::canonicalize and applies the source exactly when both name the same directory - the same lookup git performs when opening prefix/.gitignore - falling back to the prior string comparison without a root. The harness's is_source check likewise became a filesystem lookup: a written file is a live source exactly when dir/.gitignore canonicalizes to it. Contract preserved: dir_applies is called only from judge; the no-root fallback is byte-identical to the old behaviour and stays pinned by icase_folds_nested_gitignore_dir_prefix; set_repo_root is documented and set by materialize for every case.
Changed: src/lib.rs (repo_root field, set_repo_root, dir_applies as a method with filesystem resolution, docs, new test repo_root_applicability_asks_the_filesystem); src/bin/differential.rs (set_repo_root in materialize, FS-lookup is_source replacing the ASCII name check, new test unicode_source_applicability_follows_filesystem, and unicode_collision_model_mirrors_disk resequenced to take verdicts before cleanup - my iteration-1 test queried the matcher after deleting the temp tree, legal under string-compare applicability and invalid once applicability reads the disk; the pinned contract is unchanged); corpus/nested-and-layered/15.case hand-authored (5 queries); PLAN.md growth line and one Lessons line; BACKLOG.md task deleted and icase-reach Settled line closed back to class-complete.
Checkpoint: 1bb9af459c3c5f57092442b92322dee4c93472f0; PLAN.md row nested-and-layered re-ticked with this hash (its slice grew and re-swept green in the same full replay). Stall check: src/lib.rs, src/bin/differential.rs, and corpus/nested-and-layered/15.case changed outside the state files and 1 ledger item changed state, no stall.
Verification: Acceptance observed failing first: slice replay pre-fix exit 1, 15 cases, 42 queries, 1 disagreement - q="sub/a.txt" in the U+00FC spelling, matcher=not-ignored vs oracle=ignored (pattern "a.txt" at the source named in the query's own spelling, line 1); exact-spelling, cross-case-unmatched, and sharp-s anti-overfold controls all agreed pre-fix. Fix attempt 1 of 3. Post-fix: slice 15 cases, 42 queries, 0 disagreements; full Verify exit 0 with output redirected to a file and the real exit status read, 104 cases, 291 queries, 0 disagreements, 0 harness failures; corpus grew 103 to 104, monotone. cargo test first run caught the iteration-1 test's stale sequencing (verdicts after cleanup) - repaired in this iteration, then 17 tests across 4 suites all ok (was 15, two added); the full Verify was green both before and after that test-only repair, so no product behaviour moved with it. cargo tree crate-only.
Learnings: Two folds live on the verdict path and must never mix - ASCII WM_CASEFOLD for pattern text against the query's spelling, the volume's table for source discovery through real lookups; recorded in PLAN Lessons. A test that consults the filesystem must take its verdicts before cleaning up its tree; the iteration-1 test assumed pure string applicability and this iteration's change invalidated that assumption, caught by the suite on first run.
Next: ledger is empty and no full fresh-evidence audit has run this run - iteration 3 audits per the Scoped-mode Method (Definition of done position), which decides closeout and sets up the evaluator gate.
